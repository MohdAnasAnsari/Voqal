apiVersion: v1
kind: Service
metadata:
  name: voqal-frontend
  namespace: voice-ai
  labels:
    app: voqal-frontend
spec:
  selector:
    app: voqal-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
